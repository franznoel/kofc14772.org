{
  "name": "stgenknights",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Website for Knights of Columbus St. Genevieve Council 14772",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "dashboard:build": "npm run --prefix dashboard build",
    "dashboard:deploy": "npm run --prefix dashboard deploy",
    "dashboard:logs": "npm run --prefix dashboard logs",
    "dashboard:migrate:latest": "npm run --prefix dashboard db:migrate"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
